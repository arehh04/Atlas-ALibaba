No build or install steps — the skill is loaded directly by Claude Code via its skill mechanism. For accurate API field/schema answers, configure the MCP service with `npx @anthropic-ai/claude-code mcp add atlas-docs --type url --url https://resources.atriptech.com/~gitbook/mcp`; if MCP is unavailable, the skill falls back to WebFetch against `https://resources.atriptech.com/?ask=...` and then to its built-in knowledge.